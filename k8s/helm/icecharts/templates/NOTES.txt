IceCharts has been deployed!

{{- if .Values.ingress.enabled }}

Access the application at:
  {{- if .Values.ingress.tls.enabled }}
  https://{{ .Values.ingress.host }}
  {{- else }}
  http://{{ .Values.ingress.host }}
  {{- end }}

API endpoint:
  {{- if .Values.ingress.tls.enabled }}
  https://{{ .Values.ingress.host }}/api/v1/health
  {{- else }}
  http://{{ .Values.ingress.host }}/api/v1/health
  {{- end }}
{{- else }}

Port-forward to access locally:
  kubectl port-forward svc/{{ include "icecharts.fullname" . }}-web {{ .Values.web.port }}:{{ .Values.web.port }} -n {{ include "icecharts.namespace" . }}
  kubectl port-forward svc/{{ include "icecharts.fullname" . }}-api {{ .Values.api.port }}:{{ .Values.api.port }} -n {{ include "icecharts.namespace" . }}
{{- end }}

Check pod status:
  kubectl get pods -n {{ include "icecharts.namespace" . }}

View logs:
  kubectl logs -l app.kubernetes.io/component=web -n {{ include "icecharts.namespace" . }}
  kubectl logs -l app.kubernetes.io/component=api -n {{ include "icecharts.namespace" . }}
