{{- /*
NetworkPolicy for Redis
- Allows ingress from API pods on port 6379
- Allows DNS egress for external resolution
- Denies all other traffic
*/ -}}
{{- if .Values.networkPolicies.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: icecharts-redis-policy
  namespace: {{ include "icecharts.namespace" . }}
  labels:
    {{- include "icecharts.labels" . | nindent 4 }}
    app.kubernetes.io/component: cache
  annotations:
    description: "NetworkPolicy for Redis cache - allows ingress from API pods"
spec:
  podSelector:
    matchLabels:
      {{- include "icecharts.redis.selectorLabels" . | nindent 6 }}
  policyTypes:
    {{- toYaml .Values.networkPolicies.policyTypes | nindent 4 }}
  ingress:
    # Allow ingress from API pods on port 6379
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/component: backend
      ports:
        - protocol: TCP
          port: 6379
  egress:
    # Allow DNS egress for name resolution
    - to:
        - namespaceSelector: {}
          podSelector:
            matchLabels:
              k8s-app: kube-dns
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
{{- end }}
