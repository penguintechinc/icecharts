apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: icecharts-prod

resources:
  - ../../base
  - secret.yaml
  - ingress.yaml

patches:
  - path: configmap-patch.yaml
  - path: resource-patch.yaml
